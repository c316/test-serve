{
  "name": "test",
  "version": "0.0.1",
  "dependencies": {
    "parcel-bundler": "^1.8.1",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "serve": "^8.2.0"
  },
  "scripts": {
    "now-build": "parcel build ./public/index.html -d ./build",
    "now-start": "serve --single ./build",
    "now-deploy": "now",
    "dev": "parcel ./public/index.html -d ./build --no-cache"
  },
  "engines": {
    "node": "< 10.0.0",
    "npm": ">= 5.6.0"
  }
}
